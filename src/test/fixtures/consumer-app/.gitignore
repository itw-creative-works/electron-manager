# Runtime-only artifacts. The boot runner symlinks node_modules (electron-manager +
# electron) and webpack-builds dist/ on each self-test run — none of it is committed.
node_modules/
dist/
logs/
release/
